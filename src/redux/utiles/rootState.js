import {apiResponses, loader, user, layout, table} from "./initialStore";

const rootState = {
    user,
    apiResponses,
    // loader,
    layout,
    table,
};

export default rootState;
